NODE_ENV=local # Options: local, development, production

# Note: All other configuration (Database, JWT, etc.) is fetched from
# Firebase Remote Config during application startup for non-local environments.

# Telegram admin notifications — for local dev, set directly (or LOCAL_-prefixed).
# In development/production these are expected in Firebase Remote Config as
# DEV_TELEGRAM_BOT_TOKEN/DEV_TELEGRAM_CHAT_ID or PROD_TELEGRAM_BOT_TOKEN/PROD_TELEGRAM_CHAT_ID.
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Content source for device types / subscription plans & addons / service parts / welcome
# bonus — historically Strapi (console), now watchtower's own Strapi-compatible GraphQL
# (/graphql) + REST (/api/*) endpoints. Point at your local watchtower dev server and a
# token minted via `npx tsx scripts/create-api-token.ts <name> read-only` in watchtower.
STRAPI_URL=http://localhost:4000
STRAPI_API_TOKEN=

# Seconds a CMS read (device types / plans / addons / parts / welcome bonus) stays cached
# in-memory before being re-fetched from watchtower. Watchtower also actively invalidates
# on write via POST /cache/invalidate (see src/routes/cache.route.ts) — this TTL is only a
# backstop for the rare case that call is missed.
CACHE_TTL_SECONDS=300
